const usage = `import AccentFrame from "@/lib/components/ui/frames/AccentFrame";\n\nexport default function App() {\n  return (\n    <div className="flex flex-col gap-8">\n      {/* Default: expand on hover, duo corners */}\n      <AccentFrame color="cyan" className="text-white text-center">\n        Hover to expand\n      </AccentFrame>\n\n      {/* Quad corners + dual color + glow */}\n      <AccentFrame\n        color="cyan"\n        colorB="pink"\n        mode="quad"\n        hoverEffect="glow"\n        glowIntensity="high"\n        className="text-white text-center"\n      >\n        Quad dual-color glow\n      </AccentFrame>\n\n      {/* Always-animated pulse */}\n      <AccentFrame\n        color="pink"\n        mode="quad"\n        hoverEffect="pulse"\n        animated\n        bgVariant="subtle"\n        className="text-white text-center"\n      >\n        Always pulsing\n      </AccentFrame>\n\n      {/* Rounded corner tips, trace effect */}\n      <AccentFrame\n        color="green"\n        cornerStyle="rounded"\n        hoverEffect="trace"\n        cornerLength={24}\n        cornerThickness={3}\n        className="text-white text-center"\n      >\n        Rounded trace\n      </AccentFrame>\n    </div>\n  );\n}`;

export default usage;
