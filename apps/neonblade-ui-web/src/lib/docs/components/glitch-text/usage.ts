const usage = `import GlitchText from "@/lib/components/ui/text/GlitchText";\n\nexport default function App() {\n  return (\n    <div className="flex flex-col gap-8">\n      {/* children-only — text prop is inferred automatically */}\n      <h2 className="text-4xl font-bold font-orbitron uppercase text-white">\n        <GlitchText mode="hover">Hover Glitch</GlitchText>\n      </h2>\n\n      {/* active + heavy + fast */}\n      <h2 className="text-4xl font-bold font-orbitron uppercase text-white">\n        <GlitchText mode="active" intensity="heavy" speed="fast">Heavy Fast</GlitchText>\n      </h2>\n\n      {/* chaos + custom colors */}\n      <h2 className="text-4xl font-bold font-orbitron uppercase text-[#ff6600]">\n        <GlitchText\n          mode="active"\n          intensity="chaos"\n          colorA="#ff6600"\n          colorB="#ffe000"\n          offset={4}\n        >\n          Chaos\n        </GlitchText>\n      </h2>\n\n      {/* neon glow + flicker */}\n      <h2 className="text-4xl font-bold font-orbitron uppercase text-[#00f3ff]">\n        <GlitchText\n          mode="active"\n          neon\n          neonFlicker\n          glowColor="cyan"\n          intensity="heavy"\n        >\n          Neon\n        </GlitchText>\n      </h2>\n\n      {/* explicit text prop — use when children is JSX, not a plain string */}\n      <h2 className="text-4xl font-bold font-orbitron uppercase text-white">\n        <GlitchText text="Styled" mode="hover">\n          <span className="text-cyan-400">Styled</span>\n        </GlitchText>\n      </h2>\n\n      {/* multi-colour preset — spread scales with offset */}\n      <h2 className="text-4xl font-bold font-orbitron uppercase text-white">\n        <GlitchText mode="active" palette="spectrum" offset={3}>Spectrum</GlitchText>\n      </h2>\n\n      {/* persistent split + custom layers (a multi-colour wordmark) */}\n      <h2 className="text-5xl font-bold font-orbitron uppercase italic text-white">\n        <GlitchText\n          mode="active"\n          rest="split"\n          layers={[\n            { color: "#fb40e9", x: -5, y: 2 },\n            { color: "#03c3ff", x: 3, y: -3 },\n            { color: "#000000", x: 2, y: -2 },\n          ]}\n        >\n          Shine\n        </GlitchText>\n      </h2>\n    </div>\n  );\n}`;

export default usage;
