const usage = `import { NotchCard } from "@/lib/components/ui/cards/NotchCard";\nimport { Cpu, Zap, Shield } from "lucide-react";\n\nexport default function App() {\n  return (\n    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">\n      {/* Default: top + bottom notches, cyan border */}\n      <NotchCard\n        title="Cyber Unit"\n        description="Default top & bottom notches with cyan accent."\n        icon={<Cpu className="w-full h-full" />}\n      />\n\n      {/* Gradient border + pink accent + beam */}\n      <NotchCard\n        borderGradient\n        borderColor="pink"\n        borderColorB="cyan"\n        accentColor="pink"\n        beamVariant="single"\n        beamColor="pink"\n        title="Power Core"\n        description="Gradient border with a spinning beam overlay."\n        icon={<Zap className="w-full h-full" />}\n      />\n\n      {/* All four sides + custom notch geometry + scan hover */}\n      <NotchCard\n        notchSides={["top", "bottom", "left", "right"]}\n        notchSize={16}\n        notchWidth={60}\n        notchWidthV={30}\n        borderColor="green"\n        accentColor="green"\n        hoverEffect="scan"\n        glowIntensity="high"\n        title="Defense Grid"\n        description="Notch on all sides with custom H/V widths and scan hover."\n        icon={<Shield className="w-full h-full" />}\n      />\n\n      {/* Rainbow beam + children slot + centered */}\n      <NotchCard\n        beamVariant="rainbow"\n        notchSides={["top", "bottom"]}\n        notchWidth={80}\n        notchSkew={20}\n        hoverEffect="lift"\n        align="center"\n        title="Neural Link"\n      >\n        <div className="mt-3 font-mono text-xs text-green-400/80 space-y-1">\n          <p>&gt; boot sequence complete</p>\n          <p>&gt; all systems nominal</p>\n          <p className="animate-pulse">&gt; _</p>\n        </div>\n      </NotchCard>\n    </div>\n  );\n}`;

export default usage;
